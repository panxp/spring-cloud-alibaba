#server:
#  port: 518
#order.yaml
spring:
  application:
    name: order
  cloud:
    nacos:
      config:
        file-extension: yaml
        group: DEFAULT_GROUP
        namespace: public
        prefix: ${spring.application.name}
        server-addr: 127.0.0.1:8848
#  profiles:
#    active: dev